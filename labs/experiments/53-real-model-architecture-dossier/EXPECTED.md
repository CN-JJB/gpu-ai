# Expected — Experiment 53

There is no universal real-model answer.

A valid dossier:
- identifies model/config revision;
- derives attention/KV/FFN structure;
- identifies MoE/caveats if present;
- uses exact artifact bytes when available;
- labels parameter×bpw as a proxy otherwise;
- never upgrades `POSSIBLE-NOT-PROVEN` to a confirmed fit;
- leaves PP/TG as hypotheses until real benchmark evidence exists.
