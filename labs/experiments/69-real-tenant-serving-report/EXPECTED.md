# Expected — Experiment 69

No universal real result.

A valid report:
- uses pseudonymous tenant identity;
- uses exact prompt-token counts where token accounting is claimed;
- separates request share from token/resource share;
- reports latency per tenant;
- does not infer unsupported runtime quota/scheduler behavior.

Fairness is a policy decision plus measured resource/latency allocation.
