# Expected — Experiment 55

No universal real-model result.

Correct behavior:

- ordinary full attention → homogeneous formula is usable;
- one `sliding_window` field without layer-pattern evidence → do not silently assume all layers local;
- explicit classified `layer_types` → per-layer hybrid sum may be used;
- MLA proxy requires explicit opt-in and exact architecture confirmation;
- unknown patterns remain UNKNOWN.
