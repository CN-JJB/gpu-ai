# Expected — Experiment 77

No universal real incident.

A valid packet:
- contains a shared timeline;
- preserves raw server metrics;
- records telemetry sampling interval;
- stores raw vendor output instead of silently guessing unsupported fields;
- includes client-visible symptoms;
- separates hypothesis from proven cause;
- changes no clocks/power/driver/network configuration.

High GPU/VRAM values alone are not an incident conclusion.
