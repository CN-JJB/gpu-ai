{
  "semantic_source_probe_schema_version": 1,
  "working_directory": ".",
  "probes": [
    {
      "probe_id": "llama-bench-version",
      "purpose": "capture exact llama-bench runtime/build identity from the executable intended for Experiment 61",
      "argv": ["llama-bench", "--version"],
      "required": true,
      "accepted_returncodes": [0],
      "timeout_seconds": 20
    },
    {
      "probe_id": "llama-bench-help",
      "purpose": "capture flag semantics supported by this exact llama-bench build before constructing benchmark argv",
      "argv": ["llama-bench", "--help"],
      "required": true,
      "accepted_returncodes": [0],
      "timeout_seconds": 20
    },
    {
      "probe_id": "llama-bench-list-devices",
      "purpose": "capture device/backend identities visible to this exact llama-bench build when the option is supported",
      "argv": ["llama-bench", "--list-devices"],
      "required": false,
      "accepted_returncodes": [0],
      "timeout_seconds": 20
    },
    {
      "probe_id": "llama-perplexity-help",
      "purpose": "capture quality-evaluation flags supported by the exact installed llama-perplexity build",
      "argv": ["llama-perplexity", "--help"],
      "required": true,
      "accepted_returncodes": [0],
      "timeout_seconds": 20
    }
  ]
}
