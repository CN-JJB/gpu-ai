# Expected — Challenge 04

完成时你应该能：
- 不看答案解释 program_id/offset/mask；
- 把 kernel 映射到 memory + execution model；
- 先证明 correctness；
- 用 exact shape/dtype/version 做 benchmark；
- 解释 fusion/tiling 的 tradeoff；
- 拒绝无条件“某语言更快”结论。

真机暂缺可标 DEFERRED-HARDWARE。
