# Expected — Challenge 12

合格结果必须：
- 不把多模态当单一 text-model artifact；
- 保存 model + projector/encoder + media identity；
- 把 preprocessing 和 visual/audio tokens 放进 reproducibility；
- 分析额外 memory/context；
- 有多类型 quality fixture；
- 说明 privacy boundary；
- 对 rapidly changing runtime pin exact commit。
